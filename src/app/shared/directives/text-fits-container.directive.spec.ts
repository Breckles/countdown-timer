import { TextFitsContainerDirective } from './text-fits-container.directive';

describe('TextFitsContainerDirective', () => {
  it('should create an instance', () => {
    // const directive = new TextFitsContainerDirective();
    // expect(directive).toBeTruthy();
  });
});
